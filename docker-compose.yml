name: e-sale

services:
  sale:
    build: .
    ports:
      - "8084:8084"
    networks:
      - ecommerce-network
networks:
  ecommerce-network:
    name: ecommerce-network
    driver: bridge
    external: true